/*
    This is the smallest code engine possible. It does nothing but declare a variable.
    Hoisting is not supported in this engine, so the variable is not accessible outside of the block scope.
 */
{
  var a = 10;  
}